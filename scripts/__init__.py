"""Standalone data-prep and training-glue scripts for the "freeze the
architecture, validate on real data" pass. Not engine or application code --
run each as a module (`uv run python -m scripts.<name>`) from the repo root."""
